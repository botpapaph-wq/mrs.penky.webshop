# Stand 11.08.2026, mittags

## Zuerst: meine Diagnose von gestern war falsch

Ich hatte geschrieben, PayPal habe den Webhook zugestellt und ein fehlender
Header `paypal-transmission-id` habe zur Ablehnung geführt. Das war eine
Vermutung, keine Messung.

Die Wahrheit ist einfacher: **die deployte Funktion kannte PayPal überhaupt
nicht.** Sie stammte noch aus der Stripe-Zeit — 10 KB, eine einzige Datei,
kein einziges Vorkommen von „paypal" im Code. Das Repo war seit Tagen weiter
als das, was tatsächlich lief.

Gemessen, nicht geraten: der Kopfkommentar der deployten Fassung lautete
*„receives PayMongo / Stripe payment webhooks"*.

## Behoben und geprüft

**`payment-webhook` neu deployt** — beide Dateien, `index.ts` und
`_shared/paypal.ts`. Verhalten vorher und nachher:

| Anfrage | vorher | jetzt |
|---|---|---|
| PayPal-förmig, ohne Signatur | 400 „No valid signature" | **403** — Signaturprüfung greift |
| unbekannte Nutzlast | 400 | 400 |

403 statt 400 ist der Beweis, dass die Weiche jetzt in den PayPal-Zweig
führt und die Prüfung dort tatsächlich läuft.

**Stolperstein für später:** beim Deploy über das Dashboard liegen die
Dateien flach nebeneinander, der Import muss `./_shared/paypal.ts` heißen.
Im Repo steht `../_shared/…`, weil dort ein echtes Elternverzeichnis
existiert. Mit dem falschen Pfad antwortet der Deploy mit HTTP 400 und
keiner brauchbaren Meldung. Steht jetzt als Kommentar in der Datei.

**Migration 007 ausgeführt:**

```
14 Titel geändert · 0 Wortdoppelungen · 0 "Cross-border" · 2 Titel noch doppelt
```

Weg sind „Pendant Pendant", „bracelet bracelet", „Rosary Rosary",
„candle candle" und der Handelsjargon. Die alten Titel liegen in
`penky_title_backup`, Rücknahme steht am Ende der Migrationsdatei.

Zwei Paare bleiben gleichnamig, weil beide Artikel dasselbe Material in der
Beschreibung nennen:

- `Cross pendant necklace – Alloy` (397b5559, 41ddca0f)
- `Jesus cross pendant – Titanium` (8228cf3c, 9b52d8d9)

Das sind vermutlich echte Dubletten aus dem CJ-Import. Einen Unterschied zu
erfinden wäre falsch — entweder du deaktivierst je einen, oder sie bleiben.
Deine Entscheidung.

**CJ-Konto:**

- Standard-Versandart Philippinen: 1. CJPacket Asia Liquid Line,
  2. CJPacket Asia Ordinary
- Empfängeradresse gespeichert und als Standard markiert. CJ **zeigt sie
  nicht an** — reiner Anzeigefehler, über die Schnittstelle ist der
  Datensatz da. Dabei sind drei identische Einträge entstanden; der als
  Standard markierte ist der vollständigste.

## Noch offen

| | |
|---|---|
| CJ-Geldbörse | 0,00 € — ohne Guthaben fertigt CJ keine Bestellung ab |
| `INTERNAL_FUNCTION_SECRET` | fehlt; ohne das ruft der Webhook `forward-order` nicht auf |
| PayMongo | ID-Verifizierung läuft |
| PayPal Live | erst nach erfolgreichem Sandbox-Durchlauf |
| Sandbox-Kauf | muss jemand mit Anmeldung durchklicken |
| 2 doppelte Titel | Entscheidung offen |
| 2 überzählige CJ-Adressen | löschen? |

Für einen vollständigen Durchlauf fehlt jetzt nur noch ein bezahlter
Sandbox-Kauf. Die Kette dahinter steht zum ersten Mal komplett.
